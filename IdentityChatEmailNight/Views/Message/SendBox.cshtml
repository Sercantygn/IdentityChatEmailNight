@model List<IdentityChatEmailNight.Entities.Message>
@{
    Layout = "~/Views/Shared/_MessageLayout.cshtml";
    int count = 0;
}

<table class="table table-bordered">
    <tr>
        <th>#</th>
        <th>Alıcı Email</th>
        <th>Konu</th>
        <th>Mesajı Aç</th>
    </tr>
    @foreach (var item in Model)
    {
        count++;
        <tr>
            <td>@count</td>
            <td>@item.ReceiverEmail</td>
            <td>@item.Subject</td>
            <td><a href="/Message/MessageDetail/@item.MessageId" class="btn-warnin">Mesajı Aç</a></td>
        </tr>
    }

</table>